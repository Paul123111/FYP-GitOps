---
apiVersion: agones.dev/v1
kind: Fleet
metadata:
  name: planet-hunter-fleet
spec:
  template:
    spec:
      health:
        initialDelaySeconds: 120
        periodSeconds: 20
        failureThreshold: 5
      ports:
        - name: default
          portPolicy: Dynamic
          containerPort: 7777
      lists:
        players:
          capacity: 4
      template:
        spec:
          nodeSelector:
            node-type: workers
          containers:
            - name: unity-simple-server
              image: {{ .Values.awsId }}.dkr.ecr.eu-west-1.amazonaws.com/planet-hunter-server:1.53
              imagePullPolicy: IfNotPresent
              resources:
                requests:
                  memory: 250Mi
                  cpu: 100m
                limits:
                  memory: 400Mi
                  cpu: 2000m

